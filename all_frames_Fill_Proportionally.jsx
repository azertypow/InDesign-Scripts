main();function main() {	var myDocument = app.activeDocument;	var myPages = myDocument.pages;	for (myCounter = 0; myCounter <= myDocument.pages.length - 1; myCounter++) {		myPage = myPages.item(myCounter);		//myDocument.align(myPage.pageItems, AlignOptions.LEFT_EDGES);		for (var i = 0; i <= myPage.pageItems.length - 1; i++) {			//alert(myPage.pageItems[i])			myPage.pageItems[i].fit(FitOptions.FILL_PROPORTIONALLY);		}	}}